# Backend URL — set this in Vercel's project settings
# (Project → Settings → Environment Variables)
# Leave blank to use the same-origin proxy (local dev only)
VITE_API_URL=https://your-app.up.railway.app/api
